# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# Set user's SFTP env settings here, which are picked up for user's session
# after they complete authentication
export SFTP_HOST=localhost

# User specific aliases and functions
